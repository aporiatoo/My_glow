# راهنمای ایجنت — قبل از هر تغییری این را کامل بخوان

این پروژه توسط چند ایجنت به‌صورت نوبتی توسعه داده می‌شود. هر ایجنت بدون
حافظه از نوبت قبل شروع می‌کند. این فایل تنها منبع حقیقت دربارهٔ «چطور»
کار کردن با این کد است.

---

## ۱. اول این سه دستور را بزن

```bash
cd engine
node lint.js     # باید «تمیز» بدهد
node test.js     # باید PASS 37/37 بدهد
bash build.sh    # باید ASCEND.html بسازد
```

اگر هر کدام شکست، **قبل از هر کار دیگری** آن را درست کن. بیلد شکسته را
روی بیلد شکسته نساز.

---

## ۲. این پروژه چیست

اپ شخصی مدیریت زندگی برای یک نوجوان ۱۴ سالهٔ ایرانی (پایهٔ نهم).
دو لایه دارد:

- **موتور وب** (`engine/`) — کل منطق و رابط کاربری. یک فایل HTML تک‌تکه
  تولید می‌کند که کاملاً آفلاین کار می‌کند.
- **پوستهٔ اندروید** (`app/`) — WebView تمام‌صفحه + اعلان + ویجت.
  منطقی در آن نیست جز پل نیتیو.

APK نهایی در `release/Ascend.apk`.

---

## ۳. معماری موتور

منطق خالص و تست‌پذیر در ریشهٔ `engine/`، رابط در `engine/src/`.

| فایل | مسئولیت |
|---|---|
| `schema.js` | نسخه‌بندی، اعتبارسنجی و مهاجرت داده. `migrate()`، `compact()` |
| `core.js` | state پایه، کوئست، پروتکل، XP، `plan()` خام |
| `routines.js` | ۲۵ میکروروتین |
| `calendar.js` | تقویم جلالی، تعطیلات، `sunTimes()` |
| `study.js` | سیستم درسی پایهٔ نهم، معدل وزنی، توصیهٔ رشته |
| `social.js` | حلقهٔ روابط، گرمای تماس |
| `tree.js` | درخت مهارت، ۲۶ گره |
| `extra.js` | آنبوردینگ، محرک وسوسه، تست بدنی، خواب، فصل، خرید، قد، خانواده، `brief()` |
| `guide.js` | ۴۶ راهنما (مثال + گام‌ها + جستجوی ویدیو) |
| `img.js` | ۴۶ اینفوگرافیک base64 |
| `systems.js` | آمادگی، مرور فاصله‌دار |

| `src/` | لایهٔ رابط |
|---|---|
| `shell.html` | اسکلت، تمام CSS، اسلات‌های `/*FONT*/` و `/*CORE*/` |
| `logic.js` | state، `ask()`، حالت‌ها، `plan()` با کش |
| `interact.js` | کشیدن انگشت، نگه داشتن، برگرداندن، ورودی سریع |
| `views.core.js` | روز، کوئست، آمار، سپر، پروتکل، پروفایل |
| `views.life.js` | مالی، روابط، پیشرفت، تمرین، تغذیه، پوشش |
| `views.sys.js` | سیستم‌های تحلیلی |
| `views.edu.js` | درسی + bootstrap + `rd()` |
| `tail.html` | بستن سند |

---

## ۴. قواعدی که نقض‌شان باگ می‌سازد

این‌ها از باگ‌های واقعی این پروژه درآمده‌اند، نه سلیقه.

**تاریخ.** همیشه `td()` یا `dstr()`. **هرگز** `toISOString().slice(0,10)`.
بین نیمه‌شب تا ۰۳:۳۰ تهران، UTC روز قبل است و ثبت روی روز اشتباه می‌افتد.
این باگ یک بار کل استریک و نقشهٔ فعالیت را خراب کرد.

**توابع `window.`.** با پیشوند صدا بزن: `window.fn()` نه `fn()`.
فراخوان برهنه در مرورگر کار می‌کند ولی در تست Node می‌شکند.
این اشتباه **چهار بار** تکرار شد. `lint.js` می‌گیردش.

**state جدید.** به `SCHEMA` در `schema.js` اضافه کن، نه به `repair()`.
اگر ساختار قدیمی ناسازگار شد، `SCHEMA_VERSION` را بالا ببر و در
`MIGRATIONS` تابع مهاجرت بنویس.

**`plan()`.** کش دارد. اگر ورودی تازه‌ای روی خروجی اثر می‌گذارد،
حتماً به `planKey()` اضافه‌اش کن وگرنه نتیجهٔ کهنه می‌دهد.

**دیالوگ.** فقط `ask()`. هرگز `alert`/`confirm`/`prompt`.

**بلوک جدید در برنامه.** هرگز مستقیم `rows.push()` نکن. از الگوی
درج امن استفاده کن که تداخل زمانی را چک می‌کند — نمونه‌اش در حالت رمضان
و حالت‌های خرابی هست. تست ۷۰ ترکیب روز×حالت را می‌سنجد.

**تایپوگرافی.** اندازهٔ خام ننویس. فقط `var(--t1)` تا `var(--t6)`.
`letter-spacing` منفی برای فارسی ممنوع — خط متصل است و حروف می‌چسبند.

**ویرایش فایل.** موقع پچ کردن با `str.replace` حتماً وجود الگو را
assert کن. پچ بی‌صدای no-op چند بار وقت تلف کرده.

---

## ۵. محدودیت‌های کاربر — قابل مذاکره نیستند

- **کدو و بادمجان** هرگز در هیچ برنامهٔ غذایی یا لیست خرید. تست می‌سنجدش.
- **باشگاه کاربر فوتبال است**، نه بدن‌سازی. هیچ بلوک بدن‌سازی نباشد.
  تمرین قدرتی خانگی با وزن بدن.
- **فوتبال:** یکشنبه، سه‌شنبه، پنجشنبه ۱۴:۰۰–۱۵:۳۰.
- **تلگرام بلاک نشود** — فقط سقف ۴۵ دقیقه.
- **هیچ عکسی از کاربر** در اپ نباشد.
- **پروتئین ۱۱۰ گرم**، کالری‌شماری عمداً حذف شده.
- **لحن:** بی‌طرف و داده‌محور. بدون شرم، بدون جملهٔ انگیزشی توخالی.
  در بخش پاکی مخصوصاً — راه‌حل مهندسی بده نه نصیحت اخلاقی.
- **رنگ:** مونوکروم سفید و سیاه.
- **زبان رابط:** دوزبانه با `L('فارسی','English')`.

---

## ۶. جریان کار

```bash
cd engine
# ویرایش کن...
node lint.js && node test.js    # هر دو باید پاس شوند
bash build.sh                    # نسخهٔ وب
bash build.sh apk                # asset اندروید (تصاویر جدا)
```

`bash sync.sh` هر چهار کار را با هم انجام می‌دهد.

**تست را کم نکن.** اگر قابلیتی اضافه کردی، تستش را هم به `test.js`
اضافه کن. عدد ۳۷ باید بالا برود نه پایین.

---

## ۷. بیلد APK

اینجا قابل انجام نیست مگر ابزارها نصب باشند. راه درست: GitHub Actions.
`.github/workflows/build.yml` اول لینت و تست را می‌زند، بعد بیلد.
اگر تست بشکند APK ساخته نمی‌شود.

**کلید امضا.** `ascend.jks` در `.gitignore` است و در مخزن نیست.
برای بیلد در CI این Secrets لازم است:
`KEYSTORE_B64`، `KEYSTORE_PASSWORD`، `KEY_ALIAS`، `KEY_PASSWORD`.

کلید باید **ثابت** بماند. اگر عوض شود، اندروید نسخهٔ جدید را اپ دیگری
می‌بیند و کاربر برای به‌روزرسانی باید نصب فعلی را حذف کند — با همهٔ داده‌اش.

---

## ۸. وضعیت فعلی

- **۳۷ تست** پاس، لینت تمیز
- **۱۸۰+ صفحه** در ۳۱ ویو و ۶ گروه
- **۴۶ راهنما** با ۲۷۶ گام و ۴۶ اینفوگرافیک
- APK حدود **۲ مگابایت**، بدون مجوز اینترنت

**انجام‌شده:** تایم‌لاین تطبیقی، کوئست و XP، درخت مهارت، سیستم درسی،
حلقهٔ اجتماعی، پشتیبان‌گیری، حالت‌های خرابی، رمضان، تحلیل اشتباهات،
حال دوبعدی، نقشهٔ فعالیت، شروع تدریجی، نقشهٔ وسوسه، تست‌های بدنی،
بدهی خواب، فصل ۱۲ هفته‌ای، لیست خرید، منحنی قد، تنفس، خانواده،
پیام صبحگاهی، کشیدن انگشت، ورودی سریع، اعلان اکشن‌دار، ویجت.

**نشده:** بلاکر اپ‌ها (VpnService + Accessibility)، Health Connect،
بیومتریک و SQLCipher، ورودی صوتی ژورنال، مهاجرت به Compose.

---

## ۹. اگر چیزی نفهمیدی

کد را بخوان، حدس نزن. `test.js` رفتار مورد انتظار را دقیق نشان می‌دهد.
`lint.js` نشان می‌دهد چه چیزی ممنوع است و چرا.

قبل از تغییر بزرگ، یک تست بنویس که رفتار فعلی را تثبیت کند.
